name: Run Design Token Build

on:
  pull_request:
    paths:
      - 'lib/design/app.tokens.json'

jobs:
  call-split-tokens:
    if: github.head_ref == 'feat/design-tokens-test'
    uses: bitmark-inc/feralfile-app/.github/workflows/split-design-tokens.yaml@main-test

  call-build-tokens:
    if: github.head_ref == 'feat/design-tokens-test'
    needs: call-split-tokens
    uses: bitmark-inc/feralfile-app/.github/workflows/parse-style-tokens.yaml@main-test
